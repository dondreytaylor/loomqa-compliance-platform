# Global, environment-driven UI mode for demos.
#
# Valid values:
# - (unset/blank)  => normal UI
# - LOADING        => show loading state UI across all pages
# - EMPTY_DATA     => show empty state UI across all pages
# - ERRORED        => show error state UI across all pages
LOOPQA_UI_STATE=
